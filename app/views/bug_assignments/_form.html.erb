<%= form_for(@bug_assignment) do |f| %>
  <% if @bug_assignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bug_assignment.errors.count, "error") %> prohibited this bug_assignment from being saved:</h2>

      <ul>
      <% @bug_assignment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :timestamp %><br>
    <%= f.datetime_select :timestamp %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
